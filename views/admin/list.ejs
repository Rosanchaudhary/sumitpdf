<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Notes - <%= subject.name %></title>
  <link rel="stylesheet" href="/stylesheets/admin.css">
</head>
<body>
  <div class="admin-container">
    <header class="admin-header">
      <h1>Manage Notes</h1>
      <nav>
        <a href="/admin">Dashboard</a>
        <a href="/admin/semesters/<%= subject.semester._id %>/subjects">Back to Subjects</a>
      </nav>
    </header>

    <main class="admin-main">
      <div class="breadcrumb">
        <a href="/admin">Dashboard</a> /
        <a href="/admin/degrees">Degrees</a> /
        <a href="/admin/degrees/<%= subject.semester.engineeringDegree._id %>/semesters">
          <%= subject.semester.engineeringDegree.name %>
        </a> /
        <a href="/admin/semesters/<%= subject.semester._id %>/subjects">
          Semester <%= subject.semester.number %>
        </a> /
        <span><%= subject.name %></span>
      </div>

      <div class="page-header">
        <h2>Notes for <%= subject.name %> (<%= subject.code %>)</h2>
        <a href="/admin/subjects/<%= subject._id %>/notes/new" class="btn btn-primary">
          + Upload New Note
        </a>
      </div>

      <div class="table-container">
        <% if (notes.length === 0) { %>
          <p class="empty-state">
            No notes uploaded yet. 
            <a href="/admin/subjects/<%= subject._id %>/notes/new">Upload the first note</a>
          </p>
        <% } else { %>
          <table class="data-table">
            <thead>
              <tr>
                <th>Title</th>
                <th>Author</th>
                <th>Upload Date</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% notes.forEach(note => { %>
                <tr>
                  <td><strong><%= note.title %></strong></td>
                  <td><%= note.author %></td>
                  <td><%= new Date(note.uploadDate).toLocaleDateString() %></td>
                  <td class="actions">
                    <a href="<%= note.pdfUrl %>" target="_blank" class="btn btn-sm">View PDF</a>
                    <a href="/admin/notes/<%= note._id %>/edit" class="btn btn-sm btn-secondary">Edit</a>
                    <form action="/admin/notes/<%= note._id %>/delete" method="POST" style="display: inline;" 
                          onsubmit="return confirm('Are you sure you want to delete this note?');">
                      <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } %>
      </div>
    </main>
  </div>
</body>
</html>
