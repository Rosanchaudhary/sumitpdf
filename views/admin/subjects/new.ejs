<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add New Subject - Admin</title>
  <link rel="stylesheet" href="/stylesheets/admin.css">
</head>
<body>
  <div class="admin-container">
    <header class="admin-header">
      <h1>Add New Subject</h1>
      <nav>
        <a href="/admin">Dashboard</a>
        <a href="/admin/semesters/<%= semester._id %>/subjects">Back to Subjects</a>
      </nav>
    </header>

    <main class="admin-main">
      <div class="breadcrumb">
        <a href="/admin">Dashboard</a> /
        <a href="/admin/degrees">Degrees</a> /
        <a href="/admin/degrees/<%= semester.engineeringDegree._id %>/semesters">
          <%= semester.engineeringDegree.name %>
        </a> /
        <a href="/admin/semesters/<%= semester._id %>/subjects">
          Semester <%= semester.number %>
        </a> /
        <span>New Subject</span>
      </div>

      <div class="form-container">
        <h2>Create Subject for Semester <%= semester.number %></h2>
        
        <form action="/admin/semesters/<%= semester._id %>/subjects" method="POST">
          <div class="form-group">
            <label for="name">Subject Name *</label>
            <input 
              type="text" 
              id="name" 
              name="name" 
              required 
              placeholder="e.g., Thermodynamics, Circuit Analysis"
            >
          </div>

          <div class="form-group">
            <label for="code">Subject Code (Optional)</label>
            <input 
              type="text" 
              id="code" 
              name="code" 
              placeholder="e.g., MECH301, EE201"
              maxlength="20"
            >
          </div>

          <div class="form-actions">
            <button type="submit" class="btn btn-primary">Create Subject</button>
            <a href="/admin/semesters/<%= semester._id %>/subjects" class="btn btn-secondary">Cancel</a>
          </div>
        </form>
      </div>
    </main>
  </div>
</body>
</html>
