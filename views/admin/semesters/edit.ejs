<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Semester - Admin</title>
  <link rel="stylesheet" href="/stylesheets/admin.css">
</head>
<body>
  <div class="admin-container">
    <header class="admin-header">
      <h1>Edit Semester</h1>
      <nav>
        <a href="/admin">Dashboard</a>
        <a href="/admin/degrees/<%= semester.engineeringDegree._id %>/semesters">Back to Semesters</a>
      </nav>
    </header>

    <main class="admin-main">
      <div class="breadcrumb">
        <a href="/admin">Dashboard</a> /
        <a href="/admin/degrees">Degrees</a> /
        <a href="/admin/degrees/<%= semester.engineeringDegree._id %>/semesters">
          <%= semester.engineeringDegree.name %>
        </a> /
        <span>Edit Semester</span>
      </div>

      <div class="form-container">
        <h2>Edit Semester <%= semester.number %></h2>
        
        <form action="/admin/semesters/<%= semester._id %>/edit" method="POST">
          <div class="form-group">
            <label for="number">Semester Number *</label>
            <input 
              type="number" 
              id="number" 
              name="number" 
              value="<%= semester.number %>"
              required 
              min="1"
              max="10"
              placeholder="e.g., 1, 2, 3..."
            >
          </div>

          <div class="form-group">
            <label for="name">Semester Name *</label>
            <input 
              type="text" 
              id="name" 
              name="name" 
              value="<%= semester.name %>"
              required 
              placeholder="e.g., Odd, Even"
            >
          </div>

          <div class="form-actions">
            <button type="submit" class="btn btn-primary">Update Semester</button>
            <a href="/admin/degrees/<%= semester.engineeringDegree._id %>/semesters" class="btn btn-secondary">Cancel</a>
          </div>
        </form>
      </div>
    </main>
  </div>
</body>
</html>
